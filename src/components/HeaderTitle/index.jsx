import React from 'react';
import styled from 'styled-components/native';

const Title = styled.Text`
  color: white;
  font-size: 24px;
`;

export default function HeaderTitle() {
  return <Title>Medicamentos</Title>;
}
